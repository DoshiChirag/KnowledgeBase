'''Examples for PEP 8, the style guide for python code

While PEP8 Summary:
* Readability Counts
* Following a standard for coding style improves readability
*Consistency with your code
'''
#each group of imports is separated by a space
import os # standard lib first
import sys # one lib per line

import comments #last should be local modules
import doc_strings # do not use wildcard imports

class Layout():
	'''For each level of indentation use 4 spaces'''
	'''Use spaces based indentation'''
	''' Limit each line text to 72-79 chars'''

	def meaningful_method_name(
		firstparam,
		secondparam.
		...
	)
		pass

	def read_write(self):
			


