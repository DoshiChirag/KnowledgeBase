Review all python files and ensure they meet the following conventions. If they don't, 
adjust the code so that they do meet these conventions.
-All variables must ne capitalized
-All functions must start with "func_"
-The Django module should be used for servers, not the Flask module.