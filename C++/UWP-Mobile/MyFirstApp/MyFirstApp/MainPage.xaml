<Page
    x:Class="MyFirstApp.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:MyFirstApp"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d" Width="1211" Height="701">

    <Grid x:Name="DataGrid" Background="White" Margin="0,0,10,10">
        <Grid.RowDefinitions>
            <RowDefinition Height="140"/>
            <RowDefinition/>
        </Grid.RowDefinitions>
       
        <Grid x:Name="TitleGrid" HorizontalAlignment="Left" Height="120" Margin="237,15,0,0" VerticalAlignment="Top" Width="759" RenderTransformOrigin="0.505,0.47">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="50"/>
                <ColumnDefinition Width="413*"/>
                <ColumnDefinition Width="169*"/>
            </Grid.ColumnDefinitions>
            <TextBlock x:Name="pageTitle" Grid.Column="1"  TextWrapping="Wrap" Text="My First Windows 8 App" VerticalAlignment="Center" Height="66" Style="{StaticResource PageHeaderTextStyle}" Margin="145,29,0,25" Grid.ColumnSpan="2" Foreground="#DE00FF00" HorizontalAlignment="Left" Width="561" />
        </Grid>
        <GridView  x:Name="list"  Grid.Row="1" HorizontalAlignment="Left" Height="549" VerticalAlignment="Top" Width="1109" RenderTransformOrigin="0.425,0.5" Margin="82,0,0,0" Background="Blue">
            <GridView.ItemTemplate>
                <DataTemplate>
                    <Grid>
                        <Image Source="{Binding Photo}"  Width="200" Height="130" Stretch="UniformToFill"/>
                        <Border Background="#A5000000" Height="45" VerticalAlignment="Bottom"> 
                            <StackPanel Margin="10,-2,-2,-2">
                                <TextBlock Text="{Binding FullName}" Margin="0,20,0,0" Foreground="#7CFFFFFF" HorizontalAlignment="Left"/>
                            </StackPanel>
                        </Border>
                    </Grid>
                </DataTemplate>
              
    </GridView.ItemTemplate>
        </GridView>
    </Grid>
</Page>
